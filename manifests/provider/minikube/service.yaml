apiVersion: v1
kind: Service
metadata:
  name: verda-cloud-provider
  namespace: kube-system
  labels:
    app: verda-cloud-provider
    component: cloud-provider
spec:
  type: ClusterIP
  selector:
    app: verda-cloud-provider
  ports:
  - port: 8086
    targetPort: 8086
    protocol: TCP
    name: grpc
